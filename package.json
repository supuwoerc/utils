{
  "name": "@supuwoerc/utils",
  "type": "module",
  "version": "0.1.0",
  "author": {
    "name": "Idris",
    "email": "supuwoerc@gmail.com",
    "url": "https://github.com/supuwoerc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/supuwoerc/utils"
  },
  "keywords": [
    "utils"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.es.js",
      "require": "./dist/index.cjs.js"
    }
  },
  "main": "./dist/index.cjs.js",
  "module": "./dist/index.es.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "doc": "typedoc --options typedoc.json",
    "release": "pnpm test && pnpm build && changelogen --release --push && pnpm publish",
    "release:patch": "pnpm test && pnpm build && changelogen --release patch --push && pnpm publish",
    "release:minor": "pnpm test && pnpm build && changelogen --release minor --push && pnpm publish",
    "release:major": "pnpm test && pnpm build && changelogen --release major --push && pnpm publish",
    "release:alpha": "pnpm test && pnpm build && changelogen --release alpha --push && pnpm publish",
    "release:beta": "pnpm test && pnpm build && changelogen --release beta --push && pnpm publish",
    "release:rc": "pnpm test && pnpm build && changelogen --release beta --rc && pnpm publish",
    "lint": "eslint . --fix",
    "test": "vitest --run",
    "test:ui": "vitest --ui",
    "test:watch": "vitest",
    "test:coverage": "vitest --coverage",
    "prepare": "husky"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "throttle-debounce": "^5.0.2"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^4.19.0",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/user-event": "^14.6.1",
    "@types/lodash-es": "^4.17.12",
    "@types/node": "^22.18.12",
    "@types/throttle-debounce": "^5.0.2",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^4.0.15",
    "camelcase": "^8.0.0",
    "changelogen": "^0.6.2",
    "eslint": "^9.38.0",
    "husky": "^9.1.7",
    "jsdom": "^27.1.0",
    "lint-staged": "^16.2.6",
    "prettier": "^3.7.4",
    "terser": "^5.44.0",
    "typedoc": "^0.28.15",
    "typescript": "^5.9.3",
    "vite": "^7.1.10",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^4.0.15"
  },
  "lint-staged": {
    "*.ts": [
      "prettier --write",
      "eslint --fix"
    ]
  }
}
